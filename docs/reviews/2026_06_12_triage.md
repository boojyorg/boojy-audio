# Review-chain triage — Tyr's decisions, 2026-06-12

Owner triage of the three 2026-06-12 reports (`2026_06_12_ui_ux_review.md`,
`2026_06_12_feature_gap_review.md`, `2026_06_12_eng_health_review.md`) plus the
`2026_06_11_dogfood_notes.md` friction list. These decisions override the reports' proposals
where they differ. `docs/plans/v0.7-plan.md` is written from this file, not from the raw reports.

## Headline decisions (settled — don't relitigate)

From the 2026-06-12 scoping session:

- **v0.7 = "Devices & Feel"** — consistency + maintainability before content. The feature-gap
  review's "First Sound" proposal is the **v0.8** theme (presets/sounds/onboarding land on the
  stabilized UX).
- **v0.6.1 bug-fix patch ships FIRST** — small, certain dogfood fixes; doubles as the live
  delivery test of the PR #98 updater fix.
- **v0.7 size = medium**: the 4 ui-ux pillars + the quick-win list + riders (B4 Capture-MIDI
  wiring, B6 audio Cmd+D) + the EH-2..5/EH-9 guardrails half-day.
- **Light theme ships working** — the token pillar's definition of done.
- **Dropdown style = filled-chip** (Tyr's pick from the 06-11 mockups).
- **Engineering hardening = background track**, never a theme. Codebase-review reruns at the
  **end** of the hardening cycle to measure, not before.

Decided at this triage (Tyr, 2026-06-12):

- **v0.6.1 = "small & certain" only** — A4, A5, A8, A9, A10 (see below). The lag/teleport quick
  wins (X1/X2/M1) stay in v0.7; A3 note-height needs a repro and stays in v0.7.
- **EQ live-spectrum (D4) + reverb quality pass (D5) → v0.8** — earn the device shell first;
  they're sound-quality work that belongs in "First Sound".
- **Swing (B9): verify, then keep-or-hide** — in v0.7, verify whether Apply Swing audibly
  changes playback. If yes → keep it, restyled as a one-shot action (same treatment as Quantize,
  dogfood B3). If genuinely inert → hide it (same broken-promise-control logic as High
  Contrast). Engine groove stays parked at v1.0.
- **EH-12 repo purge runs right after v0.6.1 ships** — standalone session, nothing in flight,
  audience smallest it will ever be. (git-filter-repo history rewrite; force-push + reclone.)

---

## v0.6.1 — bug-fix patch (ships before v0.7)

Purpose: fix the certain dogfood bugs **and live-test the PR #98 updater fix** (the v0.6.1
release is the first tag where Sparkle should offer an update automatically).

| Item | Fix |
| --- | --- |
| A4 piano-roll resize-vs-move conflict | cursor shows `<|>` but drag moves the note — hit-zone/priority fix ("really frustrating") |
| A5 add-effect [+] popup far right | repro first (§F5: not a `showMenu` — locate the real widget); if it turns deep, punt to v0.7 |
| A8 off-palette default track colors | rule: defaults may ONLY come from the 16-color `manualPalette`; retire `categoryColors` drift + legacy 8-list |
| A9 wordmark "▲udio" click opens Settings | logo → Start screen; Settings via gear only; keep the red-triangle engine-health behaviour |
| A10 library hover/cursor | pointer cursor on clickable folders; "+ Add Folder" hover/press states |

Explicitly **not** in v0.6.1: zoom (needs the spec session), MIDI hot-plug (M, v0.7 Devices),
editor overflow (v0.7 shell), sampler fixes (v0.7), Windows updater (v0.7), X1/X2/M1 (v0.7
quick wins), A3 note-height (needs repro; v0.7).

**Immediately after v0.6.1:** the **EH-12 artifact purge** as its own session (`git rm --cached`
iOS `.a` libs + `build_win/` tree, gitignore, git-filter-repo, force-push, fresh clone check).

---

## v0.7 "Devices & Feel" — scope by slice

### Pillar 1 — Theme-token correctness (Light theme works everywhere)

T1, T2, T3, T4, T5, T6, T9/P6 · P4 (snap/quantize popups) · L4 (loop-dim overlay) · T8/L7 for
the **live call sites** (full `required`-param hardening = follow-up) · N7 ride-along (radius
token). T7 (CC palette) only if it proves unreadable on Light during this pass — else backlog.
**Definition of done: switch to Light and every surface reads correctly.**

### Pillar 2 — Interaction footguns (trust)

X1, X2 (the two `onDoubleTap` ~300 ms lags — `_lastTapAt` fix) · M1 (fader click-teleport).
EH-11 (TransportBar tap tests) rides along since the code is open.

### Pillar 3 — Legibility cues (GarageBand-grade)

M2 peak-hold · M3 unity tick · M4 icon+tooltip M/S/R/I · M5 sends-in-dB · M6 strip border fix ·
M9 ride-along (return-strip tooltips + delete confirm) · P1/P2/P3/P5 piano-roll lane re-treat ·
P7 tool-mode badge · P9 **audit first** (are Scale controls missing from the live build?) ·
L1 selected-clip border · L2 return-only empty-state · **BoojyTooltip** wrapper (dogfood B8 —
Affinity-style title + one-liner + shortcut; priority: Quantize, Legato, Snap, M/S/R, transport).

### Pillar 4 — One device shell (the headline)

E1–E5, E8, E9 via the shared shell (header glyph + name + power dot + collapse → visualizer →
2–3 hero params → fixed MIX knob) · E7 delete `EffectParameterPanel` (+ E10, M8/E6
confirm-and-delete) · B10 synth restyle = shell adoption (waveform visual + labelled knob row +
segmented wave icons) · A6 editor min-height = **option (c)** (devices adapt to a compact floor,
graph collapses first, then hard min — no scroll-in-scroll) · A7 + D6 sampler fix package
(waveform starts at left; Start/Length chips; informed by Tyr's C2 research). EH-8 (sampler/
drum-pad command tests) + EH-14 (sampler pitch test) ride this slice.

### Feel slice — one popup system + alive chrome

B2 dropdowns app-wide = **filled-chip** value chips + one shared compact menu panel · B4
right-click menus tightened to the same panel chrome · B3 Quantize restyled to the action role
(one-shot, press-flash, keep `▾` split) · B6 piano-roll Start/Length/Signature as matching value
chips · B5 bar-ruler [-][+] ghost glyphs · B7 hover/motion language (**spec needs Tyr's visual
sign-off before implementation**) — absorbs N5 (hover-scale unification) · N8 + B9 top-bar
group-priority overflow (affordance pick still open: trailing `»` chevron vs right-click) ·
N1, N2, N3, N4 top-bar smalls · C1 em-dash · Settings unify C2–C6 (+ C7/C8 ride-along) = B11 ·
B1 recover-backup file-card restyle (**flex item** — first to slip if the cycle swells).

### Devices (hardware) slice

A1/B10(gap) MIDI hot-plug — poll port names ~2 s, select by stable name/id (kills the
index-staleness) · A11/B11(gap)/D7 Windows updater MethodChannel wiring + macOS Settings
"Check automatically" toggle.

### Riders (too cheap to schedule)

B4(gap) **Capture MIDI** wiring to a transport button (~5 min; backend complete) · B6(gap)
audio Cmd+D duplicate · D3 **Legato** one-shot next to Quantize · CC-lane expand-toggle wiring
(nice-to-have #1) · **ghost notes** (nice-to-have #3): same keep-or-hide rule — wire it if it
stays small while the piano roll is open, else hide the inert toggle · swing verify-then-keep-
or-hide (above).

### Guardrails (half-day, background track)

EH-2 (appcast `|| echo` removal + audio-thread `println!` grep gate) · EH-3 (`cargo fmt --check`
+ `rust-toolchain.toml` pin) · EH-4 (pubspec-vs-tag version gate) · EH-5 (CompositeCommand
round-trip test) · EH-1 remainder (CI grep on the plist feed URL) · EH-15 rides along (same
release.yml: key → `$RUNNER_TEMP` + trap; DMG-existence assert) · EH-9 = **Tyr's settings
click** (require `flutter-checks` + `rust-checks` on master) · EH-17 docs batch (stale ROADMAP
heading, ARCHITECTURE tree, lint-count claim, `.gitmodules` `ignore = dirty`, social-preview.png
commit-or-gitignore, brand-PNG prune, build.sh guard).

### Tyr homework inside v0.7 (blocking their slices)

1. **Zoom spec session (C1)** — DAW comparison checklist (anchor, modifiers, pinch, ruler-drag,
   zoom-to-fit, H/V independence) → "Boojy zoom spec" → one PR. A2 + A3 land with/after this.
2. **Sampler research (C2)** — before the sampler slice.
3. **Hover/motion spec sign-off (B7)** and the **B9 overflow affordance pick**.

### Scope-pressure note

As triaged, v0.7 is at the top of "medium" (ui-ux estimated 6–9 days for its subset alone; the
Feel slice + hardware slice add more). The committed core = the 4 pillars + riders + guardrails.
**Flex zone (first to slip to v0.7.x):** B1 recover-backup card, B9 overflow affordance, the full
hover/motion sweep (can ship on the most-touched surfaces only). Steady small batches with
walkthrough check-ins between, per the standing working style.

---

## v0.8 "First Sound" — parked (the next theme, already chosen)

B1 thin synth presets (Piano/Strings/Bass/Pad/Lead on the existing 1-osc engine + re-enable the
built preset browser — ship thin, no multisamples) · B2 named effect patches · B3 guided
first-song onboarding (infra ~85% built; sequenced after B1) · D2 **Capture Audio** (prove the
Capture-MIDI UX in v0.7 first) · D4 EQ live spectrum + D5 reverb quality pass (moved here at
this triage) · templates (#7) + drum-machine preset patterns (#8) · tempo-synced preview (#9)
and the loop library (#10) gated on B1 content.

## Backlog / v1.0 / opportunistic

- **v1.0 (re-affirmed):** B7 clip normalize (decided 2026-06-08) · B8 pan automation (stays
  hidden — no engine; don't "fix") · B9 swing engine groove · LUFS platform selector (decided
  2026-06-05) · i18n.
- **Deliberately deferred, revisit at v0.9/pre-1.0 scoping:** B5 loop recording / take comping —
  the one engineering-heavy parity gap; the review's own "most defensible single deferral".
- **Backlog (unscheduled):** T10 fontSize migration · T11 canvas-bg resolution · T7 CC palette
  (unless Light forces it) · X3, X5–X8 transport leftovers · P8 zoom-gutter discoverability ·
  L3, L5, L6, L8 timeline smalls · M7 add-return affordance · C9–C12 start-screen batch (incl.
  C10 sync thumbnail reads) · drum velocity lane (#5) · variable step res/choke (#11) · PDC
  (#12) · crossfades (#13 — design shift, not a primitive) · undo history panel (#14) ·
  EH-13 (adelay VST3 test) · EH-16 (macOS CMake rebuild in CI).
- **Opportunistic when the subsystem is next touched:** EH-6 (automation interpolation tests) ·
  EH-7 (FFI null-safety tests) · EH-10 (tempo re-push test).
- **Out / settled — don't re-raise:** N6 panel-toggle active state (quiet chrome, rejected
  treatment stands) · social/cloud/collab/share-sheet (feature-gap §4) · Linux (revisit v0.8 if
  asked) · Web (v0.9+ strategic question) · automation shapes · step-sequencer swing ·
  the eng-health "What NOT to do" list (no tracing crate, no Windows smoke rig, no gesture-layer
  widget tests, no realtime render-callback tests).
- **Closed as already shipped** (feature-gap §4): virtual piano (D8 = discoverability, fold into
  tooltips/onboarding) · drag-and-drop import · MP3/WAV/stem export · unlimited undo · input
  monitoring/count-in/punch.

---

## Report corrections noted at triage

- The ui-ux report's quick-win list says "16 quick wins" but enumerates ~22 IDs, calls the
  snap/quantize popup fix "P7" once (it's **P4**; P7 is the tool badge), and its §8 in/out lists
  drift slightly from §3 (N4, N8). This triage's slice lists are the canonical reading.
- The eng-health EH-1 mechanism was corrected same-day (see that report's editor's note): the
  feed URL resolved fine; the real bug was semver-vs-build-number in `sparkle:version`, fixed in
  PR #98. Only the CI grep remains, folded into the guardrails batch.
- Feature-gap framed its theme for "after v0.4.0" (stale brief wording); its substantive v0.7
  proposal was evaluated against the live v0.6.0 and adopted as **v0.8**.
