# v0.6.0 release screenshots

Visual record of the app as shipped in v0.6.0 (2026-06-11). Copied from the review staging set
(`docs/reviews/_screenshots/`) before that folder was wiped post-review.

One folder per release going forward (`docs/screenshots/vX.Y.Z/`). The flat
`screenshot_vX.Y.Z.png` files in the parent folder are the README hero shots — different job.
